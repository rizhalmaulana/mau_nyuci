name: maunyuci_core
description: "Project Laundry berbasih POS dengan Fitur pemesanan dari Customer"
version: 1.0.0
homepage:

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Network & Real-Time
  dio: ^5.4.0
  signalr_netcore: ^1.3.5

  # Local Database
  drift: ^2.14.0
  sqlite3_flutter_libs: ^0.5.18
  path_provider: ^2.1.2
  path: ^1.8.3

  # Security & Formatter
  flutter_secure_storage: ^9.0.0
  intl: ^0.19.0

  # Permissions & Location
  permission_handler: ^12.0.1
  geolocator: ^10.1.0
  firebase_messaging: ^16.6.0
  flutter_local_notifications: ^20.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
  drift_dev: ^2.14.0

flutter:
